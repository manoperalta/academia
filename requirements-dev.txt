# Dependencias de desenvolvimento e teste (nao vao para a imagem de producao).
# As dependencias de RUNTIME ficam em requirements.txt -- inclusive as de API e banco,
# que antes estavam aqui e faziam a imagem de producao subir sem elas.
-r requirements.txt

# Qualidade e testes
pytest>=8.3,<9
pytest-django>=4.9,<5
pytest-cov>=6.0,<7
factory-boy>=3.3,<4
ruff>=0.8,<1
