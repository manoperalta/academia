"""Area do aluno (PWA)."""

from django.apps import AppConfig


class AreaDoAlunoConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "area_do_aluno"
    verbose_name = "Area do aluno (PWA)"
